defmodule Euler.Exercise_071 do
  @moduledoc """
  Ordered Fractions

  By listing the set of reduced proper fractions fpor d <= 1_000_000 in ascending order of size, find the numerator of the fraction
  immediately left of 3/7
  """
  def timer(), do: :timer.tc(__MODULE__, :solution, [])

  def solution() do
  end
end
